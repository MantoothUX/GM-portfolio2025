# Project Rules

## Copy Changes
Whenever copy is changed on any page, the change must be applied to **both the desktop and mobile layouts**. Page1.tsx uses conditional rendering (`isMobile`) with separate JSX branches for each — always update both branches.
